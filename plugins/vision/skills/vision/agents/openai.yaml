interface:
  display_name: "Vision"
  short_description: "Build the outcome and check that it works"
  default_prompt: "Use $vision to take this outcome end to end."

policy:
  allow_implicit_invocation: false
